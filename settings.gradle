pluginManagement {
    repositories {
        mavenLocal()
        maven { url "http://art.nwie.net/artifactory/libs-release" }
        gradlePluginPortal()
    }
}

rootProject.name = 'reactive-app'
